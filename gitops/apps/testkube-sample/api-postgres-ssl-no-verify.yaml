apiVersion: apps/v1
kind: Deployment
metadata:
  name: testkube-samples-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: PGSSLMODE
              value: no-verify
