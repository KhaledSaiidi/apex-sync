apiVersion: batch/v1
kind: Job
metadata:
  name: garage-internal-secrets-bootstrap
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/sync-wave: "-10"
    argocd.argoproj.io/hook-delete-policy: HookSucceeded,BeforeHookCreation
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 300
  template:
    spec:
      serviceAccountName: garage-backup-bootstrap
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bootstrap
          image: bitnami/kubectl:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          env:
            - name: GARAGE_NAMESPACE
              value: garage
          resources:
            requests:
              cpu: "${ARGOCD_ENV_resource_garage_bootstrap_job_requests_cpu}"
              memory: "${ARGOCD_ENV_resource_garage_bootstrap_job_requests_memory}"
            limits:
              cpu: "${ARGOCD_ENV_resource_garage_bootstrap_job_limits_cpu}"
              memory: "${ARGOCD_ENV_resource_garage_bootstrap_job_limits_memory}"
          command:
            - /scripts/internal-secrets-bootstrap.sh
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
      volumes:
        - name: script
          configMap:
            name: garage-internal-secrets-bootstrap-script
            defaultMode: 0555
