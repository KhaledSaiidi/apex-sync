apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: istio-internal-ingress-gw-keep-alive
  namespace: istio-gateways
spec:
  workloadSelector:
    labels:
      istio: istio-internal-ingress-gw
  configPatches:
    - applyTo: LISTENER
      match:
        context: GATEWAY
      patch:
        operation: MERGE
        value:
          socket_options:
            - description: enable TCP keepalive
              int_value: 1
              level: 1
              name: 9
              state: STATE_PREBIND
            - description: start keepalive probes after 5 minutes of idle time
              int_value: 300
              level: 6
              name: 4
              state: STATE_PREBIND
---
apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: istio-external-ingress-gw-keep-alive
  namespace: istio-gateways
spec:
  workloadSelector:
    labels:
      istio: istio-external-ingress-gw
  configPatches:
    - applyTo: LISTENER
      match:
        context: GATEWAY
      patch:
        operation: MERGE
        value:
          socket_options:
            - description: enable TCP keepalive
              int_value: 1
              level: 1
              name: 9
              state: STATE_PREBIND
            - description: start keepalive probes after 5 minutes of idle time
              int_value: 300
              level: 6
              name: 4
              state: STATE_PREBIND
