apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '22'
  labels:
    app.kubernetes.io/name: keycloak-config
    app.kubernetes.io/part-of: platform-baseline
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: ${ARGOCD_ENV_gitops_root_app_repo_url}
    targetRevision: ${ARGOCD_ENV_gitops_root_app_target_revision}
    path: gitops/apps/keycloak/keycloak-config
    plugin:
      name: envsubst
      env:
        - name: base_domain
          value: "${ARGOCD_ENV_base_domain}"
        - name: root_app_repo_url
          value: ${ARGOCD_ENV_gitops_root_app_repo_url}
        - name: root_app_target_revision
          value: ${ARGOCD_ENV_gitops_root_app_target_revision}
        - name: keycloak_operator_version
          value: "${ARGOCD_ENV_keycloak_operator_version}"
  destination:
    server: https://kubernetes.default.svc
    namespace: keycloak
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 10
      backoff:
        duration: 5s
        maxDuration: 3m0s
        factor: 2
    syncOptions:
      - PrunePropagationPolicy=background
      - PruneLast=true
