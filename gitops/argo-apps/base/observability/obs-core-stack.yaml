apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: obs-core-stack
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '25'
  labels:
    app.kubernetes.io/name: obs-core-stack
    app.kubernetes.io/part-of: platform-baseline
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: ${ARGOCD_ENV_gitops_root_app_repo_url}
    targetRevision: ${ARGOCD_ENV_gitops_root_app_target_revision}
    path: gitops/apps/observability/obs-core-stack
    plugin:
      name: envsubst
      env:
        - name: base_domain
          value: "${ARGOCD_ENV_base_domain}"
        - name: mimir_version
          value: "${ARGOCD_ENV_mimir_version}"
        - name: loki_version
          value: "${ARGOCD_ENV_loki_version}"
        - name: tempo_version
          value: "${ARGOCD_ENV_tempo_version}"

  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m0s
        factor: 2
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=background
      - PruneLast=true
      - RespectIgnoreDifferences=true
      - ServerSideApply=true
