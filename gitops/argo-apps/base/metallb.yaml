apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '-10'
  labels:
    app.kubernetes.io/name: metallb
    app.kubernetes.io/part-of: platform-baseline
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/KhaledSaiidi/kube-signal.git
    targetRevision: HEAD
    path: gitops/apps/metallb
    plugin:
      name: argocd-lovely-plugin-v1.0
      env:
        - name: metallb_mode
          value: "$ARGOCD_ENV_METALLB_MODE"
        - name: ARGOCD_ENV_METALLB_ADDRESSES
          value: '$ARGO_ENV_metallb_addresses'
  destination:
    server: https://kubernetes.default.svc
    namespace: metallb-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m0s
        factor: 2
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=background
      - PruneLast=true
